# Exercícios da Aula 03

## 🛠 Repositórios, Commits e Histórico

### Nível: Básico

1.  **Iniciando a Jornada**:
    - Crie uma pasta chamada `meu-primeiro-git` e entre nela pelo terminal.
    - Transforme essa pasta em um repositório Git. Qual o comando utilizado?

2.  **O Primeiro Commit**:
    - Crie um arquivo `index.html` com qualquer conteúdo.
    - Adicione o arquivo à **Staging Area** e realize o commit com a mensagem: "Adiciona index.html inicial".

### Nível: Intermediário

3.  **Rastreando o Estado**:
    - Crie um arquivo `estilos.css` mas **não** o adicione ao Git ainda.
    - Digite um comando para verificar o estado atual do repositório. Em qual cor e seção o arquivo `estilos.css` aparece?

4.  **Visualizando o Passado**:
    - Realize o commit do arquivo `estilos.css`.
    - Visualize o histórico de commits do seu repositório. Como você faria para ver esse histórico de forma resumida (em uma única linha por commit)?

### Nível: Desafio

5.  **A Pasta Oculta**:
    - Procure na sua pasta de projeto por uma pasta oculta chamada `.git`.
    - O que acontece se você deletar essa pasta e rodar o comando `git status` em seguida? (Dica: não faça isso em projetos reais!).

---

[:octicons-arrow-right-24: Ver Solução](solutions/solucao-03.md)
